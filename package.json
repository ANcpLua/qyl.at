{
  "name": "qyl.at",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check && tsc --noEmit && node scripts/check-dependencies.mjs",
    "build:site": "astro build && pagefind --site dist",
    "build": "npm run check && npm run build:site",
    "test:unit": "node --test tests/vitals.test.ts",
    "test:gates": "node scripts/check-artifacts.mjs && npm run test:unit && playwright test --project=surface",
    "test:perf": "playwright test --project=performance",
    "test": "npm run build && npm run test:gates",
    "smoke": "npm test",
    "preview": "wrangler dev",
    "deploy": "npm test && wrangler deploy --var QYL_SERVICE_VERSION:$(git rev-parse --short HEAD)",
    "deploy:dry": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/exporter-logs-otlp-http": "^0.222.0",
    "@opentelemetry/resources": "^2.11.0",
    "@opentelemetry/sdk-logs": "^0.222.0",
    "web-vitals": "^6.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/mdx": "8.0.0",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^26.4.1",
    "@types/pngjs": "^6.0.5",
    "astro": "7.3.1",
    "fontaine": "^0.8.1",
    "pagefind": "^1.5.2",
    "pixelmatch": "^7.2.0",
    "pngjs": "^7.0.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "8.2.2",
    "wrangler": "^4.129.0"
  }
}
