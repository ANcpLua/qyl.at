{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>ANcpLua/github-settings-automation"
  ],
  "description": "Pin the Bun that Renovate runs to the packageManager version so regenerated bun.lock files keep the lockfileVersion CI (setup-bun with bun-version-file: package.json) can read.",
  "constraints": {
    "bun": "1.4.2"
  }
}
