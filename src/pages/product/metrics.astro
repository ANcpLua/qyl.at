---
import ProductPage from "../../components/ProductPage.astro";
---

<ProductPage
  title="Metrics"
  eyebrow="Product · metrics"
  intro="Standard OTLP in. Every series kept, every query aggregated server-side—through an MCP tool or the dashboard's metrics view."
  heading="Series you can ask about, not points you have to sum yourself."
  description="The collector retains metric points in DuckDB as a series index plus a point table. A query names a metric, matches attributes, bounds a time range and picks a step, and gets buckets back—never raw points. Percentiles need a histogram; OTLP summary points are declined with a partial_success that names the instrument, because pre-computed quantiles cannot be re-aggregated over a window or merged across series."
  capabilities={[
    { name: "list_metrics", description: "List the metric descriptors recorded for a project: name, instrument, unit." },
    { name: "get_metric_series", description: "List the distinct attribute streams recorded under one metric name." },
    { name: "query_metric", description: "Aggregate one metric over a time range into buckets of step_ms, with attribute matchers." },
    { name: "OTLP /v1/metrics", description: "Ingest binary protobuf or OTLP JSON over HTTP, plus the official OTLP gRPC service." },
  ]}
  command={`qyl up
# point an OTLP exporter at http://127.0.0.1:4318`}
/>
